# 开发辅助工具安装检查报告

检查时间：2026-05-24  
检查目录：`D:\CodexProjects\housekeeping-system\Staff entry mini-program`

## 1. 结论汇总

| 工具 | 检查结果 | 说明 |
|---|---|---|
| Node.js | 已安装 | 命令可用：`node --version` 返回 `v24.16.0`。 |
| npm | 已安装 | 命令可用：`npm --version` 返回 `11.13.0`。 |
| pnpm | 已安装 | 用户在 PowerShell 中确认 `pnpm -v` 返回 `11.2.2`；Codex 内部调用偶发超时，以本机 PowerShell 结果为准。 |
| Git | 已安装 | 命令可用：`git version 2.54.0.windows.1`。 |
| 当前目录 Git 仓库 | 已初始化 | 用户已执行 `git init`；当前仓库位于 `D:\CodexProjects\housekeeping-system\Staff entry mini-program\.git`。 |
| PostgreSQL 服务 | 已安装并运行 | 检测到 Windows 服务 `postgresql-x64-18`，状态为 Running。 |
| PostgreSQL 客户端 `psql` | 已安装但未进 PATH | 已找到 `D:\PostgreSQL\bin\psql.exe`；命令行直接输入 `psql` 仍不可用。 |
| VS Code | 已安装 | `code` 命令存在，路径为 `D:\家政半去中心化\Microsoft VS Code\bin\code.cmd`。 |
| 微信开发者工具 | 已安装 | 注册表检测到 `微信开发者工具 2.01.2510280`。 |
| Apifox | 已安装 | 检测到 `D:\软件\Apifox\Apifox.exe`。 |
| DBeaver | 已安装 | 检测到 `C:\Users\Administrator\AppData\Local\DBeaver\dbeaver.exe`。 |
| pgAdmin | 未检测到 | 已有 DBeaver，pgAdmin 可不安装。 |
| Docker Desktop | 未检测到 | 阶段一可选，不是必须。 |

## 2. 目前最需要处理的项

### 2.1 pnpm 已确认可用

项目根 `package.json` 已指定 pnpm。用户在 PowerShell 中确认：

```text
pnpm -v
11.2.2
```

Codex 内部调用 `pnpm --version` 偶发超时，不影响你在正常 PowerShell 中使用。后续让 Claude Code 执行 `pnpm install` 前，可在同一个终端先执行一次 `pnpm -v` 确认。

### 2.2 Git 仓库已初始化，但还没有提交

当前目录已经执行 `git init`。仓库位置：

```text
D:\CodexProjects\housekeeping-system\Staff entry mini-program\.git
```

建议下一步先不要直接 `git add .`，因为当前目录有备份 Word 文档和大量 0 字节占位源码文件。等 Claude Code 补齐阶段 0 后，再做第一次提交会更干净。

如果需要让中文文件名在 Git 状态中正常显示，可以在 PowerShell 中执行：

```powershell
git config core.quotepath false
```

如果遇到 `.git/config` 权限问题，关闭可能占用该目录的编辑器/终端后重试。

### 2.3 PostgreSQL 和 DBeaver

PostgreSQL 服务已运行：

```text
postgresql-x64-18 Running
```

DBeaver 已安装：

```text
C:\Users\Administrator\AppData\Local\DBeaver\dbeaver.exe
```

当前 `psql` 不在 PATH，但已找到可执行文件：

```text
D:\PostgreSQL\bin\psql.exe
```

阶段一开发不强制需要把它加入 PATH，只要：

- DBeaver 能连接 PostgreSQL。
- Claude Code / 后端能通过 `DATABASE_URL` 连接数据库。

如后续需要命令行直接输入 `psql`，再把 `D:\PostgreSQL\bin` 加入 PATH。

已创建开发数据库：

```text
housekeeping_staff
```

已验证连接：

```text
database: housekeeping_staff
user: postgres
```

已生成本地 `.env`，并确认 `.env` 被 `.gitignore` 忽略，不会出现在 Git 未跟踪文件列表中。

### 2.4 Docker Desktop

未检测到 Docker Desktop。阶段一不是必须。

如果后续想用 Docker 跑 PostgreSQL、MinIO、Redis，再安装：

`https://www.docker.com/products/docker-desktop/`

## 3. 已确认可用

### Node.js

```text
node --version
v24.16.0
```

### npm

```text
npm --version
11.13.0
```

### Git

```text
git --version
git version 2.54.0.windows.1
```

### VS Code

命令存在：

```text
D:\家政半去中心化\Microsoft VS Code\bin\code.cmd
```

### Apifox

检测到：

```text
D:\软件\Apifox\Apifox.exe
```

### 微信开发者工具

注册表检测到：

```text
微信开发者工具 2.01.2510280
```

## 4. 对 Claude Code 编码的影响

当前可以让 Claude Code 进入阶段 0 编码前准备，但建议先确认 PostgreSQL 连接信息。

建议处理顺序：

1. 在 PowerShell 中确认 `pnpm -v` 仍返回版本。
2. 用 DBeaver 测试 PostgreSQL 连接。
3. `.env` 已生成，`DATABASE_URL` 指向本地 `housekeeping_staff`。
4. 让 Claude Code 先完成阶段 0：工程骨架 + Prisma schema。

阶段 0 编码前最低要求：

- `node --version` 可用。
- `npm --version` 可用。
- `pnpm -v` 可用。
- 当前目录已初始化 Git 仓库。
- PostgreSQL 连接信息明确，开发数据库 `housekeeping_staff` 已创建。
