请先阅读当前项目目录下的 CLAUDE.md 和 ClaudeCode_编码交接说明.md。

本轮只完成阶段 0：项目可运行骨架。

目标：
1. 补齐 apps/server、apps/admin、apps/miniapp 的基础可运行结构。
2. 补齐 apps/server/prisma/schema.prisma。
3. 确认 pnpm install 后项目可以启动基础服务。
4. 不开发支付、押金、订单、分销、派单、大家评评理。
5. 不修改需求文档、技术方案文档、阶段计划文档，除非用户明确要求。
6. 不把旧版参考文档、备份文档或暂存目录内容重新引回项目。
7. 完成后输出：改了哪些文件、如何安装依赖、如何启动、还有哪些未完成。

约束：
- 以独立服务人员入驻小程序为唯一当前业务范围。
- 阶段 0 只做可运行基础架构和数据库 schema，不做完整业务闭环。
- 如果发现现有 0 字节占位文件，请补齐为有效代码或说明为什么暂时保留。
- 如果需要新增依赖，请优先使用当前 package.json 和 pnpm-workspace.yaml 的工程结构。
- 数据库使用 PostgreSQL，连接信息从 .env 读取，不要在代码中硬编码密码。
